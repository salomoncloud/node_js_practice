/* first project in node.js */
const mission = 'getting a cloud job';
if (mission === 'getting a cloud job') {
    console.log('you will need programming skills!');
} else {
    console.log(`Is ${mission} a challenge?`);
}